fix(diagrama): CIUDAD real en facturas (antes mostraba el codigo del cliente)

Verificacion end-to-end pedida por el usuario (Asignacion -> Diagrama -> imprimir):
- Nuevo campo Orden.ciudad (schema.plan.prisma, db push+generate OK), poblado
  desde Cliente.provincia (maestro TAT) en guardarFacturaTat(). Antes las
  facturas TAT/Inversiones mostraban en "CIUDAD" el valor de Orden.destino,
  que en TAT es el codigo interno NIT-sucursal (clave de agrupacion para
  asignacion), NO una ciudad real -- se veia literalmente el NIT del cliente
  donde debia ir "Barranquilla"/"Malambo"/etc. Bug encontrado al verificar
  visualmente el PDF real generado con datos de produccion.
- diagramaDocs.ts: ambas plantillas (Agropecuaria/Inversiones) usan
  primera.ciudad en vez de tc(primera.destino); import de tc() removido (ya
  no se usa en el archivo, el nombre del cliente ahora va en mayusculas tal
  cual, no en title-case).
- Verificado end-to-end con datos reales de produccion: se asignaron 2
  ordenes reales (1 Agropecuaria + 1 Inversiones, TAT) a un vehiculo desde
  Asignacion de ordenes, se cargaron en Diagrama, y se genero/reviso la
  factura impresa de cada una contra los datos crudos de la base de datos:
  montos por producto y totales (incluyendo IVA 5% y total con impuesto para
  Inversiones) coinciden exactamente con Orden.valor/cantidadKg, CIUDAD ya
  muestra la ciudad real, codigo de producto/CUFE/QR/firma digital OK.
- Tambien verificado en vivo contra la API real de Siesa (facturas de HOY,
  ambas companias): npedido_sigcom/tipo_entrega (feature nueva de SIGCOM)
  SI llega correctamente con el prefijo "TIPO_ENTREGA:DESPACHO" en pedidos
  creados despues del despliegue de esa feature (confirmado con 3 facturas
  reales); cufe/qr/firma_digital vienen pobladas en el 100% de las facturas
  de hoy. Vendedor sigue sin resolverse (ver nota en preferences.md), no es
  bug de esta sesion.
- Re-cargadas en vivo TODAS las facturas de hoy para poblar ciudad en
  registros existentes: 32/32 Agropecuaria, 7/7 Inversiones.
- tsc limpio en frontend y backend.
